const { Termar } = require ("./Termar")
const { TCommand } = require ("./Command")
const { TConfiguration } = require ("./Configuration")

module.exports = {
    Termar,
    TCommand,
    TConfiguration,
}
